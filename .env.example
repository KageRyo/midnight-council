# docker compose reads this file automatically after you copy it to .env.
# Only change values that differ from the defaults in docker-compose.yml.

# HOST_PORT=8080
# TRUSTED_PROXIES=10.0.0.0/8
# ALLOWED_ORIGINS=https://game.example.com
# ROOM_IDLE_TIMEOUT=30m
# MAX_ROOMS=1000
# MAX_SPECTATORS_PER_ROOM=24

# WS_CONNECTIONS_PER_SECOND=2
# WS_CONNECTION_BURST=10
# WS_MAX_CONNECTIONS_PER_IP=20
# WS_ROOM_CREATIONS_PER_SECOND=0.2
# WS_ROOM_CREATION_BURST=5

# WS_CHAT_EVENTS_PER_SECOND=1
# WS_CHAT_BURST=5
# WS_GAME_EVENTS_PER_SECOND=5
# WS_GAME_BURST=10
